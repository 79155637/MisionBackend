// Here is letting a soft type in functions, It depends of my criteria, also I can return and specifiv type like this example below
function suma(a, b): number{
  return a + b
}

const resultado = suma (4, "3")

console.log(resultado)